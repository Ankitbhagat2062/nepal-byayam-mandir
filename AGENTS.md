# Antigravity Multi-Agent Orchestration Blueprint

## 1. Lead UI/UX Visionary (Agent: Designer-Agent)
- **Role:** Owns visual layout aesthetics, token mapping, component sizing systems, and accessibility guidelines.
- **Primary Focus:** Enforces the Elite Luxury Light Gallery system setup (`#FDFCF9` canvas bases, stone-washed container layers, and gold tracking lines).
- **Output Validation:** Manages font scale responsiveness, interactive micro-interaction hover loops, and ensures large, crisp legibility optimized for older institutional operators.

## 2. Senior Frontend Architect (Agent: Architect-Agent)
- **Role:** Owns Next.js 15 architectural compilation pipelines, state logic mappings, and routing design.
- **Primary Focus:** Structures file-based systems (Layout blocks, page interfaces, automated configuration loops). Integrates the Next.js Metadata API and handles dynamic frequency selection flows across the 9 structural membership tiers.
- **Output Validation:** Validates full React Compiler optimization compatibility and checks that code compiles cleanly without build errors.

## 3. Interactive WebGL & Kinetic Engineer (Agent: Motion-Agent)
- **Role:** Owns premium animations, smooth inertia scrolling curves, and WebGL depth configurations.
- **Primary Focus:** Builds low-latency, lightweight background canvas assemblies that react cleanly to mouse movement trajectories without shifting core layout templates.
- **Output Validation:** Enforces async boundary wrapper containment (`<Suspense>`) to guarantee high-performance Core Web Vitals marks (Lighthouse Score >= 95).

## 4. Local SEO & Data Schema Engineer (Agent: SEO-Agent)
- **Role:** Owns search visibility optimization, schema payloads, data crawlers, and AI-discoverability integrations.
- **Primary Focus:** Embeds robust JSON-LD structured schema packages (`ExerciseGym` specifications) and configures sitemap generation workflows.
- **Output Validation:** Verifies structural semantic tag alignment (proper `H1` -> `H2` -> `H3` hierarchy) and maps clean social backlink bindings out to Facebook and Instagram channels.

## 5. Security & Hardware API Specialist (Agent: Backend-Agent)
- **Role:** Owns internal operational dashboard state, automation notification engines, and external infrastructure integrations.
- **Primary Focus:** Develops the CRM panel UI workflows, handling subscription expirations, member birthday notification layouts, and physical webhook lock triggers (Computerized Proximity Hardware Access logs).
- **Output Validation:** Secures route isolation patterns (ensuring private operational folders are declared in `robots.ts` disallow configurations).